spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fire_brigade?serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver

server:
  port: 8644

mybatis:
  mapper-locations: classpath:/static/mybatis/mapper/*.xml
  type-aliases-package: com.zgxf.fireman.bean
  config-location: classpath:/static/mybatis/mybatis-config.xml
